on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "Testing actions"
        run: pwd
      - name: "Testing Printing"
        run: echo Shebooo
